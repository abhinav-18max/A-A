# Optional, local interactive test image. Build the normal worker image first.
FROM mba-worker:0.2.0-arm64
USER root
RUN apt-get update && apt-get install -y --no-install-recommends x11vnc novnc websockify \
    && rm -rf /var/lib/apt/lists/*
